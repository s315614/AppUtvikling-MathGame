<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bas.livssyklus">

    <application
        android:allowBackup="true"
        android:icon="@drawable/onefeather"
        android:label="@string/app_name"
        android:roundIcon="@drawable/onefeather"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        android:hardwareAccelerated="false">
        <activity android:name=".Hoved">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Spill" />
        <activity android:name=".Preferenser" />
        <activity android:name=".Resultat" />

        <activity android:name=".Statistikk"></activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>